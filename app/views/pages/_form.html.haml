= form_for @page do |f|
  -if @page.errors.any?
    #error_explanation
      %h2= "#{pluralize(@page.errors.count, "error")} prohibited this page from being saved:"
      %ul
        - @page.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label 'Charity Name:'
    = f.text_field :name
  .field
    = f.label "Charity's Google Checkout Merchant ID:"
    = f.text_field :google_checkout_merchant_id
  .actions
    = f.submit 'Save'
